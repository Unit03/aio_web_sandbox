# A(sync)io web sandbox

Sandbox for asyncio-based web app.

# Running

Either by Docker Compose:
```bash
docker-compose up
```

or locally:
```bash
pip install -e .
foo
```

# Environment variables

See `foo/settings.py` for supported environment variables and their defaults.
